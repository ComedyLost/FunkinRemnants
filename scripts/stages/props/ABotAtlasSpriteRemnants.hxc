package funkin.remnants.stages.props;

import funkin.graphics.FunkinSprite;

class ABotAtlasSpriteRemnants extends FunkinSprite
{
  function new(x:Float, y:Float)
  {
    super(x, y);

    loadTextureAtlas("characters/abot-remnants/abotSystem", "shared");
  }
}
